[project]
name = "backend"
version = "0.1.0"
description = "Agentic LLM-Ops backend"
requires-python = ">=3.11"

[project.dependencies]
fastapi = "^0.110"
uvicorn = {extras=["standard"], version="^0.29"}
sqlalchemy = "^2.0"
pydantic = "^2.5"
psycopg = "^3.1"
boto3 = "^1.34"
celery = "^5.3"
redis = "^5.0"
python-dotenv = "^1.0"
pygithub = "^1.59"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0"
