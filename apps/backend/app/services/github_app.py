"""GitHub App integration stub."""


def open_pr(repo: str, branch: str, files: list[dict]) -> str:
    """Pretend to open a pull request and return URL."""
    return f"https://github.com/{repo}/pull/1"
